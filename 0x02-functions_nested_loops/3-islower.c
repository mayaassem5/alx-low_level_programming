#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
 *print_alphabet - prints an output
 *
 *void: has nothing
 *
 *Return: 0 always
*/

int _islower(int c)
{
	int r;

	if (islower(c))
	{
		_putchar(r + '0');
	}else
	{
		_putchar(r + '0');
		_putchar('\n');
		return (0);
	}
}
