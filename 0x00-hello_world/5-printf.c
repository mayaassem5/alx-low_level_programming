#include <stdio.h>

/**
 *main - printf "with proper grammar, but the outcome is a piece of art,"
 *
 *void: it has nothing in there
 *
 * Return: 0 always
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
